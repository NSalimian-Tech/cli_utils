[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cli_utils_najme"
version = "0.1.0"
description = "A colorful CLI package with ASCII boxes"
readme = "README.md"
requires-python = ">=3.8"